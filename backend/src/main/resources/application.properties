# Server port: Fly provides PORT env var
server.port=${PORT:8080}

# Supabase Postgres (set via Fly secrets)
spring.datasource.url=jdbc:postgresql://db.aktpbvvtnjpjeleegvox.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=AsdCat@2026#357

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Optional: keep startup fast / avoid noisy logs
spring.main.banner-mode=off
logging.level.org.springframework=INFO
